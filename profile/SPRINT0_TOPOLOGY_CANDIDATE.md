# Sprint 0 deployment topology candidate

This candidate proposes two consolidated private implementation workspaces:

- `prometheus-ui` — operator UI and web-hApp frontend;
- `prometheus-ops` — deploy, observability, backup, restore, incident and release operations.

The proposal does not remove, rename or supersede any existing private repository. It remains subject to collision discovery, repository-owner review and canonical topology approval.
